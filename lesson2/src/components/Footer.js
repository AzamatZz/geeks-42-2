import React from 'react';
import Title from "./Title";

const Footer = () => {
    return (
        <div>
            <Title title={'Footer'}/>
        </div>
    );
};

export default Footer;
const sum = (a,b) => {
    return a+b
}
console.log(sum(5, 4));
